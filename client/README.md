# LocalFixr Client

Local development is wired to the local API only.

From the project root:

```sh
npm run dev
```

Client: http://localhost:5173

API: http://localhost:5000/api

Run the client by itself:

```sh
npm run dev:client
```
